name: Build AIFI Mod

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Configure CMake for C++
        run: cmake -B AIFI-Native/build -S AIFI-Native

      - name: Build C++ DLL
        run: cmake --build AIFI-Native/build --config Release

      - name: Move DLL to Java Resources
        shell: bash
        run: |
          mkdir -p src/main/resources/natives
          cp AIFI-Native/build/Release/aifi_engine.dll src/main/resources/natives/

      - name: Generate Gradle Wrapper Safely
        shell: bash
        # Fix: Temporarily rename build.gradle so Gradle 9.6.0 doesn't crash while generating the 8.4 wrapper
        run: |
          mv build.gradle build.gradle.bak
          gradle wrapper --gradle-version 8.4
          mv build.gradle.bak build.gradle

      - name: Build Fabric Mod with Gradle
        shell: bash
        run: ./gradlew build

      - name: Upload Finished Mod
        uses: actions/upload-artifact@v4
        with:
          name: AIFI-Mod
          path: build/libs/*.jar
